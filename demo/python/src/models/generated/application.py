# generated by datamodel-codegen:
#   filename:  schema-application.yaml

from pydantic import AnyUrl, BaseModel, ConfigDict
from datetime import datetime


class Urls(BaseModel):
    model_config = ConfigDict(
        populate_by_name=True,
    )
    homepage: AnyUrl = "https://domain.com"


class RepoInfo(BaseModel):
    """
    Basic infos about repo - parses `pyproject.toml`
    """

    model_config = ConfigDict(
        extra="allow",
        populate_by_name=True,
    )
    name: str
    description: str
    version: str
    urls: Urls


type JsonTypeBasic = bool | str | int | float | datetime | None
"""
Extended basic json types
"""


type JsonType = JsonTypeBasic | list[JsonType] | dict[str, JsonType]
"""
Recursive type based on extended basic json types
"""


type Exports = RepoInfo


type ExportsModel = JsonTypeBasic | JsonType


type Imports = Exports | ExportsModel
